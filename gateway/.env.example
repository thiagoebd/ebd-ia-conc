# Entra ID (Microsoft) — copie pra gateway/.env e preencha
AZURE_CLIENT_ID=
AZURE_TENANT_ID=

# CORS — origens permitidas (vírgula entre URLs, sem espaço)
CORS_ORIGINS=http://localhost:5173

# Porta do uvicorn
GATEWAY_PORT=8000
